function toggleInfo(id) {
  let info = document.getElementById(id);
  info.classList.toggle('d-none');
}
function toggleEpisodes() {
  let episodes = document.getElementById('episodesList');
  episodes.classList.toggle('d-none');
}